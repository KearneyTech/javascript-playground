import Sample from "../../sample"

let sample = new Sample("hello");

describe('My First Test', () => {
    it('Does not do much!', () => {
        expect(trying.returnTrue()).to.equal(true)
    })
  })